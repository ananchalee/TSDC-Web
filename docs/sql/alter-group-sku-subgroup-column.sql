-- =============================================================================
-- Print Tracking Group SKU — เพิ่มคอลัมน์กลุ่มย่อย
-- ตาราง : TSDC_PICK_CHECK_NEW_TRACKING_GROUP_SKU
-- =============================================================================
--
-- SUB_GROUP_PICK = กลุ่มย่อยภายใน GROUP_PICK เดียวกัน ใช้แยกออเดอร์ที่ QTY ไม่เท่ากัน
--
-- ทำไมต้องแยก
--   GROUP_PICK ยังเป็นกลุ่มใหญ่เหมือนเดิม (เบิกของทีเดียวทั้งกลุ่ม) แต่ภายในกลุ่มอาจมี
--   ออเดอร์ที่จำนวนชิ้นไม่เท่ากัน ซึ่งใช้ขนาดกล่องคนละแบบ จึงต้อง "พิมพ์แยกรอบ"
--
-- ผลกับหน้าจอ
--   /report-printTrackingGroupSku : การ์ดแยกเป็น 1 ใบ ต่อ 1 คู่ TRANSPORT_CODE + SUB_GROUP_PICK
--                                   กดการ์ดไหนก็พิมพ์เฉพาะ sub นั้น ระบุขนาดกล่องแยกกันได้
--   /confirm-qty-groupsku         : **ไม่สน** SUB_GROUP_PICK — นับ QTY และ order
--                                   ตาม GROUP_PICK ทั้งก้อน เพราะตอนเบิกของหยิบมาทีเดียวทั้งกลุ่ม
--
-- ค่าว่าง/NULL ได้ = กลุ่มนั้นไม่ได้แบ่ง sub (ของเท่ากันหมด) การ์ดจะไม่โชว์บรรทัดกลุ่มย่อย
-- เป็นค่าที่ถูกต้องตามธุรกิจ ไม่ใช่ข้อมูลขาด จึงไม่มีการเตือนใดๆ
-- =============================================================================

USE [ชื่อ DATABASE];   -- << แก้เป็นชื่อจริงก่อนรัน
GO

-- รันซ้ำได้ ไม่พังถ้าเคยเพิ่มไปแล้ว
IF NOT EXISTS (
    SELECT 1 FROM sys.columns
     WHERE object_id = OBJECT_ID('dbo.TSDC_PICK_CHECK_NEW_TRACKING_GROUP_SKU')
       AND name = 'SUB_GROUP_PICK'
)
BEGIN
    ALTER TABLE dbo.TSDC_PICK_CHECK_NEW_TRACKING_GROUP_SKU
        ADD SUB_GROUP_PICK varchar(50) NULL;
END
GO


-- =============================================================================
-- ตัวอย่างเติมข้อมูลทดสอบ — แบ่ง sub ตามจำนวนชิ้นต่อ order
--
-- ⚠ ของจริงต้องให้ job/ระบบต้นทางเป็นคนเติม อันนี้ไว้ลองหน้าจอเท่านั้น
--   ก่อนรันจริง เปลี่ยนเป็น SELECT ดูผลก่อนเสมอ และจำกัด GROUP_PICK ที่จะทดสอบ
-- =============================================================================
/*
UPDATE g
   SET g.SUB_GROUP_PICK = 'QTY' + CAST(g.QTY AS varchar(10))
  FROM dbo.TSDC_PICK_CHECK_NEW_TRACKING_GROUP_SKU g
 WHERE LTRIM(RTRIM(g.GROUP_PICK)) = 'G-SKU002'   -- << ระบุกลุ่มที่จะทดสอบ
   AND g.QTY IS NOT NULL;
*/
